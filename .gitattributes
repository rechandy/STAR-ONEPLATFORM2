# Treat PDFs and other binaries as binary — never apply EOL normalization
# (otherwise autocrlf corrupts them on checkout).
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
