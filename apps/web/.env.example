# BFF targets — the services the Next route handlers forward to.
ROSTER_GRAPH_URL="http://localhost:3001"
STUDENT_RECORD_URL="http://localhost:3002"
SYNC_TENANT="star-demo"
# Dev stand-in for the acting staff identity (production: from the session/IAM).
SYNC_DEMO_STAFF="T0026"

# Client posts to the same-origin BFF by default; override only if needed.
# NEXT_PUBLIC_SYNC_URL="/api/sync/mutations"
